services:
  - type: web
    name: speakbetter-api
    runtime: node
    rootDir: api
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    # Add OPENAI_API_KEY in Render dashboard as a secret environment variable

